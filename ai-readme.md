# 系统架构设计

本系统用于采集、整理和输出官方媒体的时政热点与社会事件，支持结构化入库、标签化回顾以及热点导出和管理。架构分为数据采集层、处理与存储层、分析与输出层、管理与运维层四个部分。

## 架构总览
- **数据采集层**：多源采集（人民网、人民日报、半月谈、新闻联播、学习强国等），通过可配置爬虫+API 适配器实现，支持调度与重试。
- **处理与存储层**：消息队列解耦采集与处理；管道执行正文解析、去重、实体抽取、标签推断、快照生成；结果以结构化模式写入数据库与对象存储。
- **分析与输出层**：按日期/热度聚合热点，分类排序后导出 PDF 报告，支持全文检索与标签过滤。
- **管理与运维层**：统一管理后台，可配置爬取任务、监控采集状态、审查和编辑数据；提供审计日志与告警。

## 关键组件
1. **爬虫/采集服务**
   - 站点适配器：针对人民网、人民日报、半月谈、新闻联播、学习强国等提供解析器（HTML 解析、API 拉取、RSS/站点地图等）。
   - 反爬策略：请求节流、IP 轮换、UA/Headers 池，错误重试；必要时接入浏览器渲染。
   - 调度：使用 `cron`/调度器（如 Celery Beat）定义每日任务，调度参数可由管理后台配置。

2. **消息队列与任务系统**
   - 采集结果先入队（如 Redis/Celery、RabbitMQ、Kafka），异步处理，平滑突发流量。
   - 任务状态跟踪与重试策略，失败告警。

3. **数据处理管道**
   - **正文解析**：结构化提取标题、正文、来源、作者/栏目、发布时间、原文链接、多媒体链接。
   - **去重与一致性**：基于来源链接+内容指纹；版本化存储快照。
   - **NLP/标签**：关键词、实体识别（人物/机构/地点/事件），自动标签与手工标签并存。
   - **重要度判定**：结合来源权威度、发布时间、社交热度（可选外部信号）生成评分，用于“重要时政/热门事件”筛选。
   - **合规过滤**：敏感词与版权检测，存审计记录。

4. **存储设计**
   - **数据库（关系型/文档型）**：
     - `articles`：基础元数据（标题、来源、发布时间、栏目、URL、采集时间、重要度、状态）。
     - `snapshots`：与文章关联的版本化正文、摘要、正文哈希、解析时间。
     - `tags` & `article_tags`：标签字典与多对多关系（自动/手工标注来源）。
     - `attachments`：多媒体或原文 PDF/图片的对象存储位置与哈希。
     - `tasks`：采集任务配置、计划、运行日志、错误记录。
   - **对象存储**：存放页面 HTML/PDF 快照、截图、多媒体，记录版本号和哈希，方便回溯。
   - **搜索索引**：可选接入 ElasticSearch/OpenSearch，支持全文检索、标签/时间/来源过滤。

5. **分析与输出**
   - 热点聚合：按时间窗口统计阅读量/引用量/来源权重，输出“今日重要时政/法规/热门事件”列表。
   - 报告生成：按类别（时政、法规、社会热点等）排序，生成分页 PDF；支持模板化（封面、目录、摘要、详情、引用）。
   - 导出接口：API + 后台一键导出，并记录导出历史。

6. **管理后台**
   - 任务管理：创建/启停/编辑爬取任务、配置频率、来源、解析规则、反爬策略。
   - 数据管理：查看/筛选/编辑文章、快照、标签；审阅重要新闻标记；批量操作。
   - 监控与告警：采集成功率、延迟、错误率、队列深度；Webhook/邮件/IM 告警。
   - 权限与审计：角色权限控制；记录操作日志和数据修改历史。

7. **接口与集成**
   - 内部 API：数据查询、标签维护、报告导出、任务控制。
   - Webhook：供外部系统触发导出或获取热点列表。
   - 认证：Token/OAuth2，后台基于 RBAC。

## 流程说明
1. 调度器触发采集任务 → 爬虫抓取网页/API → 初步解析并入队。
2. 任务处理器消费队列 → 解析正文/媒体 → 去重、打标签、评分 → 生成快照 → 写入数据库与对象存储。
3. 分析作业定时运行 → 生成每日/周期热点榜单 → 提供 API 或后台查看 → 支持 PDF 导出。
4. 管理后台提供任务配置、数据审查、标签维护、导出和监控。

## 技术选型示例
- **后端**：Python (FastAPI/Django) + Celery；或 Node.js (NestJS) + BullMQ。
- **采集**：Requests/Playwright；解析用 lxml/BeautifulSoup；必要时使用 headless 浏览器。
- **存储**：PostgreSQL/MySQL（主存储）、S3 兼容对象存储、Redis（缓存/队列）或 Kafka。
- **搜索**：ElasticSearch/OpenSearch。
- **前端/后台**：React/Vue + Ant Design/Element Plus。
- **文档与导出**：HTML 模板 + WeasyPrint/Puppeteer 生成 PDF。
- **监控**：Prometheus + Grafana；Sentry/ELK 记录日志。

## 部署与运维
- 容器化：Docker + Compose/Kubernetes，按服务拆分（采集、处理、API、后台、队列、DB、对象存储、搜索）。
- 配置管理：使用环境变量/配置中心存放不同来源的爬取规则与凭证。
- 安全：敏感配置加密；爬虫请求限速与来源遵循 robots 协议；对外 API 需认证、限流和审计。
- 备份：数据库与对象存储定期快照；索引可重建。

## 迭代优先级
1. MVP：完成单源采集（人民网/人民日报）、结构化入库、标签与快照、基础后台管理。
2. 扩展：增加多源适配与调度配置、热点聚合与 PDF 报告、全文检索。
3. 稳定性：监控/告警、权限审计、自动化测试、回归验证。
